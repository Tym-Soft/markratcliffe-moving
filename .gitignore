# macOS
.DS_Store
**/.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Python build artefacts
__pycache__/
*.pyc
.pytest_cache/

# Local PHP server log
/tmp/php-server.log

# === Not deployed to GitHub Pages — local-only tools ===
# PHP admin panel (won't run on GH Pages — kept locally for dev only)
/admin-portal-xK7p9q/
/data/

# Local backup of the admin config + leads
ADMIN_CREDENTIALS.txt

# Local environment overrides
.env
.env.local
*.local

# OS-generated thumbnails
Thumbs.db

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example

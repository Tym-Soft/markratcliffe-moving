# macOS
.DS_Store
**/.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Python build artefacts
__pycache__/
*.pyc
.pytest_cache/

# Local PHP server log
/tmp/php-server.log

# === SENSITIVE — never commit ===
# Admin credentials (plaintext password)
ADMIN_CREDENTIALS.txt

# Admin config (contains bcrypt-hashed password — still safer not to share)
www.markratcliffemoving.co.uk/data/config.json

# Live form submissions / lead capture (if you ever add a DB or log file)
www.markratcliffemoving.co.uk/data/leads.json
www.markratcliffemoving.co.uk/data/submissions/

# Local environment overrides
.env
.env.local
*.local

# OS-generated thumbnails
Thumbs.db
